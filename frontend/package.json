{
  "name": "lifehub-frontend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "prestart": "node scripts/stamp-service-worker.js",
    "start": "react-scripts start",
    "prebuild": "node scripts/stamp-service-worker.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "vercel-build": "node scripts/stamp-service-worker.js && react-scripts build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
